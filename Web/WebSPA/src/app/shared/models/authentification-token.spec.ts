import { AuthentificationToken } from './authentification-token';

describe('AuthentificationToken', () => {
  it('should create an instance', () => {
    expect(new AuthentificationToken()).toBeTruthy();
  });
});
