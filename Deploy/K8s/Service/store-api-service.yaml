apiVersion: v1
kind: Service
metadata:
  name: store-api
  labels:
    app: my-physical-store
    env: test
    component: store-api
spec:
  ports:
  - port: 5001
  selector:
    app: my-physical-store
    env: test
    component: store-api-deploy
