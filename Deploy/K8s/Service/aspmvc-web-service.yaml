apiVersion: v1
kind: Service
metadata:
  name: aspmvc-web
  labels:
    app: my-physical-store
    env: test
    component: aspmvc-web
spec:
  ports:
  - port: 5001
  selector:
    app: my-physical-store
    env: test
    component: aspmvc-web-deploy
